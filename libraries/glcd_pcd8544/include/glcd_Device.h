/*
 * Include the PCD8544 driver.
 *
 * Arguably the driver should not be a superclass, but rather
 * a companion class with a defined interface, as creating it
 * would be simpler (and not require library changes), but this is
 * how it is for now.
 */

#include "include/glcd_Device_PCD8544.h"

